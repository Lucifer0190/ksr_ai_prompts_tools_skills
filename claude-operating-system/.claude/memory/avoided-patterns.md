# Memory — Avoided Patterns

> Anti-patterns this project has decided NOT to use, with the reason. Read before large changes
> so the same mistakes aren't reintroduced. Append newest on top. Group by area as it grows.
>
> Start empty and add patterns as you reject them. Format: `❌ **Thing.** Why it's banned + what to do instead.`

## Product / doctrine
- ❌ _Example:_ **[Anti-pattern that violates [PROJECT_DOCTRINE]].** [Why it's wrong; the doctrine-aligned alternative.]

## Engineering
- ❌ _Example:_ **[Banned technical pattern].** [Why it causes problems; the correct approach.]

## Process
- ❌ _Example:_ **Claiming "done" without verification.** Run the checklist; report honestly.
